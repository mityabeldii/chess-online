/*eslint-disable*/
import React from 'react'
import styled from 'styled-components'

import {Frame} from '../ui-kit/styled-templates'

let GuestApp = () => {
    return (
        <Frame>guest app</Frame>
    )
}

export default GuestApp;
/*eslint-enable*/